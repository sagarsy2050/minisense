# Pinned, tested runtime dependencies (generated against pyproject.toml's
# [project.dependencies] ranges). Install with:
#   pip install -r requirements.txt
# For development (tests/lint/type-check), also install requirements-dev.txt.
#
# Embeddings run through the local Ollama server (nomic-embed-text) — see
# minisense/rag/embeddings.py — so no separate HuggingFace/sentence-
# transformers download is required.

requests==2.33.1
numpy==2.4.4
faiss-cpu==1.15.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
